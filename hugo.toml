baseURL = 'https://aiagentmemory.org/'
languageCode = 'en-us'
title = 'Agent Memory — AI Memory Systems Explained'

enableRobotsTXT = true
pagination.pagerSize = 20

[markup.tableOfContents]
startLevel = 2
endLevel = 3

[related]
includeNewer = true
threshold = 80
[[related.indices]]
name = "tags"
weight = 100
[[related.indices]]
name = "keywords"
weight = 80

[permalinks]
articles = "/articles/:slug/"

[markup]
[markup.highlight]
lineNos = true
lineNumbersInTable = false
noClasses = false
[markup.goldmark.renderer]
unsafe = true

[taxonomies]
category = "categories"
tag = "tags"

[sitemap]
changefreq = "weekly"
priority = 0.5

[params]
description = "Deep technical guides on AI memory systems, agent architectures, RAG, embeddings, and retrieval. Built by engineers, for engineers."
dateFormat = "2006-01-02"
generateSocialCard = true

[params.seo]
defaultOgImage = "/og-default.png"
twitterHandle = ""

# Set your Google Analytics 4 measurement ID here
# Create one at https://analytics.google.com → Admin → Create Property → Web stream
[params.analytics]
googleAnalyticsId = ""
